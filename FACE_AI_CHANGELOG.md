# FACE_AI_CHANGELOG.md — 変更記録

> 最高峰化プロジェクト（2026-08-11〜）で実際に変更した内容の記録。
> 監査結果は FACE_AI_AUDIT.md、計画は FACE_AI_ROADMAP.md を参照。

---

## v6.4.0 — 計測層の土台を修正（2026-08-11）

監査(FACE_AI_AUDIT.md)の S1〜S3。**同じ顔で結果が再現しない**状態を解消。

### アスペクト比の異方性を補正（S1）
- MediaPipe の正規化座標は x=幅除算 / y=高さ除算で単位が違うのに、全処理を等方空間として
  扱っていた。解析の最初に一度だけ等方空間へ変換する `toIsotropic()` / `imageAspect()` を追加
- **実測(修正後)**: 1:1 / 4:3 / 16:9 で faceWHRatio・philtrumIdx・nasolabialIndex・browLidGap が
  完全一致（修正前は最大78%変動し、診断名もスコアも入れ替わっていた）

### 目の左右差を実際に検出できるように（S2）
- `eyeHeightDiff` は roll 補正で両目中点が水平化された後に再計算していたため**構造的に常に 0**だった
  （目の左右差が原理的に検出不能で、代わりに眉・口の"偽の左右差"に転嫁されていた）
- 「顔自身の縦軸(額→顎)に対する各目の位置」で測る方式に変更。首を傾けても不変
- **実測**: 基準顔 0.0000 → 左目を3mm上げると 0.0476 を検出

### roll 補正を等方空間で実施（S3）
- 異方空間で回転していたため顔が歪み、首を8°傾けただけで対称な顔が「左右非対称(中度)」と誤診
  （3:4 縦写真で B→D に低下、15°で「重度」）
- **実測(修正後)**: roll 0/8/15° すべてで asymmetryScore 0.0000、判定も一定

## v6.4.1 — 医療的断定の除去と顔面偏差値要素の廃止（2026-08-11）

監査の S4 / TOP10。「観察」と「原因推測」を分離し、他人との比較をやめる。

### 写真から判定できない身体内部の断定を全削除（S4）
- 問題の説明13件を書き換え: 「リンパの停滞」「頬の脂肪体が下方に落ちている」「咬筋が肥大している可能性」
  「頬筋の萎縮」「◯◯筋の弱化・過緊張が要因」等 → **「写真では〜に見えました」+ 撮影条件で
  見え方が変わる旨**の観察ベースへ
- 顔タイプの説明20件も同様に書き換え（「前方頭位と首前面の弱化で」等を排除）
- お悩み由来の説明15件、knowledge.js の該当カード、program.js のフェーズ名
  （「覚醒期(使えていない筋に火を入れる)」「弱化筋を集中で太くする」）も是正
- 筋肉名リストの見出しを「🔴こわばっている所/🟡使えていない所」（状態の断定）から
  **「🌷このテーマで"ゆるめる"場所 / 💪"動かす"場所」**へ変更し、
  「写真から筋肉の硬さや強さを測っているわけではありません」の注記を追加
- knowledge.js「むくみの90%はリンパの停滞」→ 出典のない数値を削除し「むくみと"めぐり"の関係」へ

### 顔面偏差値的な要素の廃止（TOP10）
- `scoreToPercentile()` / `erf()` を削除し「同年代で上位◯%」表示を廃止
  （根拠のない集団平均・正規分布仮定で、離散和のスコアには成立しない）
- S/A/B/C/**D** のグレード記号を廃止 → **「ととのっている / おおむね良好 / のびしろあり /
  じっくり取り組む / ここからスタート」**の状態ラベルへ
- 問題カードの「85」「55」「30」（3段階から機械生成した偽の精度）を削除し、
  **「優先度 高 / 中 / 気になれば」**へ。「重症度」という医療的語彙も廃止

### 検証
- node で合成3D顔を投影する検証ハーネスにより S1〜S3 を実測で確認
- 実ブラウザで解析→結果→30日→復元まで通し、**禁止表現の残存ゼロ・コンソールエラーゼロ**を確認

## v6.5 — パーソナライズの核を実装（2026-08-11）

監査の TOP6 / TOP7 / A7。「オーダーメイド」を看板だけでなく実装にする。

### ⭐優先が実際にメニューを変えるように（TOP6）
- `buildAnchors()` が優先キーもそれ以外も同じセットに入れていたため、⭐を付けても
  選ばれ方が変わらなかった（実測で Day1 が完全一致・30日通算で +1枠のみ）
- アンカーを **priority（⭐指定の悩みの主力4種）/ normal（それ以外の主力1種）** の2段階に分離。
  優先アンカーは使用回数に打ち消されない強さ（-2.2）で配置
- **実測**: Day1 が `chinTuck,faceLymphDrain,eyeWideOpen,mouthCornerLift` →
  `eyeWideOpen,browRaise,scalpRelease,binocularPose` に変化。
  まぶた系の登場枠が 30日で 24 → 31 に増加

### ⭐3つ目が落ちるバグを修正（A7）
- `priorityKeys` を「キー数」で `slice(0,3)` していたため、1チップが2キーを持つ場合に
  3つ目に選んだチップが丸ごと落ちていた → **チップ数で3つ**に修正

### 写真結果とお悩みの「照合」を実装（TOP7）
- これまでは写真の結果とお悩みが**併記されるだけ**で、一致・不一致の提示がなかった
- 結果画面の「お悩み」欄を **3ブロックの照合表示**に刷新:
  - ✅ **写真でも確認できました** — 「気にされていた点が計測にも表れています」
  - 🔍 **写真では強く出ていませんでした** — 「写真は光・角度・時間帯で見え方が変わります。
    ご本人が気になるなら大切なテーマなので、メニューには入れています」
  - 💡 **写真から気づいた点** — 選ばれていないが写真で見られた特徴
- ⭐指定のチップには⭐を表示し、優先度が伝わるように
- STEP03 の見出しを「ゆるめたい筋肉と鍛えたい筋肉を表示」→
  「写真から見えた特徴と、あなたのお悩みを照らし合わせて整理します」へ
- ヒーローの「21 診断指標」→「21 観察ポイント」（医療的語彙の回避）

## v6.6 — 入力品質のゲート（2026-08-11）

監査の TOP5 / A1 / A3。**写真の質が結果の質を決める**ため、上流で遮断する。

### 撮影品質を解析前にチェック（TOP5）
- `checkPhotoQuality()` を新設し、結果を出す**前**に判定:
  - 顔が画面からはみ出している / 顔が小さすぎる（顔幅が画面の22%未満）/ 画像が粗い（顔の実ピクセルが180px未満）
  - 2段階（block=再撮影を promptする / warn=注意して進む）
- block時は「もう一度撮ると、もっと正確に見られます」と具体的な理由・改善方法を提示し、
  **その場で写真を選び直せる**（「このまま進む」も選べる）
- warn は結果画面の警告枠に合流表示

### あおり・うつむき（pitch）の警告（A1）
- 自撮りで最も多い角度ずれなのに、推定も警告もされていなかった
- 顔の中心線(額→顎)の奥行き方向の倒れから推定し、大きいときだけ警告
- **角度の数値は出さない**（MediaPipe の z は相対値のため絶対角度は厳密でない）。
  「カメラを目の高さに合わせると、輪郭や口元がより正確に見られます」と行動を伝える

### 表情が入っているときは口元の指標を計測対象から外す（A3）
- 従来は笑顔を検出しても警告を出すだけで、**ほうれい線が mid→high に悪化**していた
- `mouthUnreliable` フラグを導入し、笑顔・開口時は**口角下がり／ほうれい線の判定自体を行わない**
- 「笑顔で写っているため、口角とほうれい線まわりは今回の計測から外しました」と正直に伝える

### 表情判定の閾値を実測で較正
- `widthHeightRatio > 5` は閉口時の実測が 9〜48 で**常に真になる死条件**だったため削除
- `lipGap > 0.06` は唇の厚みぶんの隙間（実測 0.06〜0.09）でも発火し、
  **無表情が openMouth と誤判定**されていた → 0.11 に修正
- **実測**: 通常の閉口が `neutral`、口角+6mm が `smile`(conf 1.00) と正しく判定されるように

## v6.7 — 30日コーチ化（途中評価とメニューの自動調整）（2026-08-11）

監査の TOP9 / A10。**初日に作った30日を固定しない**。

### 実施記録に粒度を持たせた（A10）
- これまでは「Day完了フラグ」だけで、どこまでできたか・どう感じたかが残らなかった
- `markDayDone(day, { level, feel })` に拡張:
  - level: **できた / 一部だけできた / 今日はお休み**（お休みは連続日数の対象外。記録だけ残す）
  - feel : **かんたん / ちょうどいい / きつい / 違和感あり**
- 入力負担を増やさないため、ガイドフロー完了時に**1問だけ**（体感）、
  ジャーニーバーの完了ボタンでは**1問だけ**（どこまでできたか）を聞く

### 途中評価（Day7 / 14 / 21）を実装（TOP9）
- これまで**該当コードが1件も存在しなかった**
- 該当日を完了すると自動で「📋 7日目の振り返り」を表示:
  - できた◯日 / 一部◯日 / お休み◯日、実施率◯%
  - その週の体感から導いた**次の1週間の方針**

### 実施状況に応じてメニューを自動調整
| 状況 | 調整 |
|---|---|
| 違和感の報告あり | 種目−1・軽めに切り替え（「痛みがあるときは中止してください」） |
| 実施率50%未満 | 種目−1・軽めに（「まず習慣にすることを優先します」） |
| 「きつい」が3日以上 | 強度を下げる |
| 実施率85%以上＋「かんたん」3日以上 | 種目+1・レベルアップ |
- `build30DayProgram(..., { adjustment })` で種目数（2〜7の範囲）と強度の重みに反映し、
  **その場でプログラムを再生成**する
- **実測**: 調整なし Day1=5種／強め4 → 減量+軽め 4種／強め1 → 増量+レベルUP 6種／強め6

### Day番号の取り違えを防ぐ堅牢化
- 開始・完了ボタンが**バインド時のDay番号を保持し続ける**構造だったため、
  他経路で進んだ場合にズレる可能性があった → **クリック時に現在のDayを取り直す**

## v6.8 — 生活習慣の反映・理由文の個別化・所要時間の是正（2026-08-11）

監査の A8 / B2 / B3。**「あなた専用」と書いてある部分を、実際に専用にする**。

### 生活習慣がメニューに反映されるように（A8）
- プロフィールで生活習慣を聞いておきながら、**メニュー生成に一切使われていなかった**
  （理由文でだけ触れるので「聞いた意味がない」状態）
- `LIFESTYLE_FAVORITE` を新設し、該当する習慣に対応する種目を候補プールに追加＋優先度を加点:

  | 回答 | 追加される観点 |
  |---|---|
  | スマホ姿勢が多い | 首・後頭部まわり |
  | デスクワーク中心 | 姿勢・肩まわり |
  | 硬いものをよく噛む | あご角まわりのリリース |
  | ストレスが高め | 呼吸・頭皮・眉間 |
  | 睡眠が短め | めぐり・むくみ系 |

- **実測**: 生活習慣を変えるとメニューが変わる日数が **0日 / 30日 → 30日 / 30日**

### 実所要時間を正直に（B3）
- 「1日15分」を選んでも実際は **6種・約3分**しか組まれず、逆に「3分」でも約3分と、
  時間設定がほぼ機能していなかった（種目ごとの実所要時間を見ていなかったため）
- `estMinutes()` で種目ごとの実時間を読み、`COUNT_BY_TIME` を再設計:

  | 設定 | 修正前 | 修正後 |
  |---|---|---|
  | 3分 | 4種・約2分 | **3種・約3分** |
  | 5分 | 5種・約3分 | **5種・約6分** |
  | 10分 | 6種・約3分 | **8種・約10分** |
  | 15分 | 6種・約3分 | **11種・約13分** |

- あわせて「**忙しい日は、はじめの◯種だけでもOK**」の**最低ライン**を提示（`coreCount`）。
  該当種目には「まずここだけ」バッジを付け、続かない日を"失敗"にしない
- 種目数を増やしても候補が枯れないよう、プール確保数と1日のストレッチ上限を拡張
  （`count*2+2` → `count*3+4`、ストレッチ上限 固定2 → 日数連動）
- 途中評価の調整幅の上限が **7種で頭打ち**になっていた不具合も同時に修正（→14）

### 「なぜ今日このケアなのか」を個別生成（B2）
- 理由文が**フェーズ×季節のテンプレート**だったため、30日で**実質3種類**しかなく、
  しかも「その日に何をやるか」に触れていなかった
- その日の**実際の1種目名・種目数・ゆるめる/動かすの内訳・優先テーマ**を文中に入れる方式へ変更。
  準備メモは Day1 と10日ごとだけに出し、毎日の文章が冗長にならないようにした
- **実測**: 30日で内容が重複しない理由文が **3種類 → 29種類**

## v6.9 — すでに手元にある無料の信号を使い切る（2026-08-11）

監査の B1。**追加費用0円**。同じモデルが同じ検出の中で既に計算している値のうち、
これまで「返してもらう設定にしていなかった」ものを受け取るようにした。

### ① 虹彩から実寸(mm)がわかるように
- モデルは 478点（うち10点が虹彩）を返せるのに、設定で無効にしていたため使えていなかった
- 虹彩の横径はほぼ誰でも約11.7mmなので、これを物差しにすると
  「0.047」のような単位のない数字を **「約1.2mm」** と現実の大きさで伝えられる
- **実測**: 合成顔（設計上の目間 61mm）に対し、画像サイズ・解像度を変えても
  **61.00mm を誤差0%で復元**。虹彩を持たない入力では自動的に無効化し、従来表示に戻る
- 同じ人の Before/After では同じ虹彩が基準になるため、変化量はmmで正直に出せる

### ② 表情の判定を「自作の閾値」から「モデルの出力」へ
- 従来は口角の上がり具合を閾値で見ていたため、**もともと口角が上向きの人は
  毎回「笑顔」扱いで口元の指標が除外され、下向きの人は笑っていても検出されない**
  という個人差による取りこぼしがあった
- モデルが返す52個の表情係数（blendshape）で判定するように変更
- **新規: 目を閉じた写真を検出できるように**。従来は検出手段が無く、
  つむった目に対してもまぶたの重さ・目の開き具合を平然と計測していた。
  検出時はまぶたと目元を計測対象から外し、その旨を伝える

### ③ 顔の向きを推定式から実測へ
- 従来は「鼻先の横ズレ」から左右の向きを、「額と顎の奥行き差」から上下の向きを
  推定していた（コード内にも「角度の絶対値は厳密でない」と明記していた）
- モデルの変換行列から **左右・上下・傾き** を直接取得する方式に変更
- **実測**: 合成した既知の角度に対し、左右・上下・傾きとも **誤差0.5°未満で復元**。
  実機でも画像を10/20°回した検証で、目の位置から求めた傾きと一致することを確認
- 指標一覧の「頭部ヨー」を **「顔の向き」**（例: ほぼ正面 / 左右18° / 上下 あおり14°）へ
- **撮影品質ゲートに反映**: 横向き28°超・あおり/うつむき22°超は
  **結果を出す前に**撮り直しを案内する（従来は結果を見たあとの警告だった）

### 既存機能の非回帰
- 27項目の回帰テストで、S1〜S3（アスペクト比不変・首の傾きで左右差が出ない・
  本当の左右差は検出する）が維持されていることを確認
- 3信号はいずれも **「あれば使う」** 構造。取得できない環境では従来の推定式・
  幾何判定にそのまま戻るため、動かなくなる経路が無い

## v6.10 — 条件が違う写真を「改善」と言わない（2026-08-11）

監査 Phase4 の「Before/After の比較可能性判定」。**30日続けた人に嘘の結果を返さない**。

### 撮影条件をスナップショットに残すようにした
- 同じ人でも、顔の向き・明るさ・写る大きさが変われば指標は動く。
  従来はそこを見ずに差分をそのまま「改善／悪化」と表示していた
- 解析のたびに **顔の向き（左右・上下）／平均の明るさ／顔の写る大きさ** を一緒に保存
  （v6.9 で顔の向きが実測できるようになったことで初めて可能になった）

### 条件が変わっている項目は「比較不可」と正直に出す
- どの条件がどの指標を狂わせるかを対応づけ、**項目ごとに**判定する

  | 変わった条件 | 比較を保留する項目 |
  |---|---|
  | 顔の向き（左右10°／上下8°超） | 中心軸・目/眉の高さ差・口角・フェイスライン・ほうれい線・むくみ |
  | 明るさ（45以上の差） | 肌のハリ・シワ／肌トーン |
  | 顔の写る大きさ（1.3倍以上） | 肌のハリ・シワ |

- **スコアは全指標の合成**なので、条件が1つでも変わっていれば
  「スコアの差は変化として比べられません」と表示し、成果として語らない
- 何が違ったか（例:「前回より暗い場所で撮られています」）と、
  次回どう撮れば比べられるかを具体的に案内する
- 撮影条件の記録がない過去の履歴は、判定不能として扱い比較自体は止めない（従来表示のまま）
- **実測**: 同じ顔を「暗く・小さく」撮った2枚で、肌系2項目が比較保留になり、
  スコア差(-8pt)が「条件差あり・参考値」として提示されることを実機で確認

### 併せて修正: Before/After の解説文が丸ごと切れていた
- `.ba-stage` が `正方形 + overflow:hidden` の写真比較窓なのに、
  その**中に**解析テキスト（スコア差・ハイライト・指標ごとの変化）を置いていたため、
  **テキスト873pxが窓の309pxで切り取られ、ほぼ何も読めない状態**だった
- 写真の窓とテキストを分離し、テキストを窓の下に表示するよう構造を修正

## v6.11 — 種目を76種 → 120種に拡張（飽きずに続けられるように）（2026-09-11）

**30日のあいだ同じ種目が何度も出てくる**のが、続かない一番の理由になりうるため、
既存76種と重ならない切り口を44種追加した。

### 追加した44種の内訳

| 部位 | 追加数 | 例 |
|---|---|---|
| 額・眉間 | 4 | 眉頭アップ／おでこ固定・目あけトレ／眉の骨きわ押し |
| 目 | 6 | まぶた単独トレ／下まぶた細めキープ／手のひらで目を温める |
| 頬 | 6 | ほお骨リフト／ほお ふくらませ・押し返し／目も笑う笑顔 |
| 口・唇 | 6 | ストロー吸い込み／唇の巻き込みキープ／片口角アップ |
| 舌 | 4 | 舌の吸い付けキープ／舌でほお押し／歯ぐきなぞり |
| あご | 5 | あご上げキス／あご下の押し上げ／斜めチンタック |
| エラ・こめかみ | 4 | エラの深ほぐし／あごストン（脱力）／口の中からゆるめる |
| 首・肩・姿勢 | 5 | 胸を開く／肩甲骨寄せ／壁立ち姿勢リセット |
| 全体・呼吸 | 4 | 鼻呼吸トレ／顔ぜんたいタッピング／おやすみ前の顔リセット |

追加の考え方は「同じ部位でも、鍛える／ゆるめる／抵抗をかける／感覚をつかむ で別種目にする」。
道具なしで場所を選ばないものを優先し、朝向き・夜向きを散らした。

### 実測（30日プログラムでの繰り返し具合）

| ケース | 使われる種類 | 同じ種目の最多登場 |
|---|---|---|
| たるみ+むくみ・10分 | 48種 → **70種** | 7回 → **6回** |
| 左右差・5分 | 40種 → **61種** | 6回 → **4回** |
| エラ+まぶた・15分 | 52種 → **76種** | 9回 → **7回** |
| 悩みなし・5分 | 40種 → **49種** | 6回 → **5回** |

### 個別化が薄まらないように選定ロジックを調整
- 種目が増えると「まだ使っていない種目」が常に大量にあるため、素の使用回数で比べると
  **悩みの主力種目が数回で追い出されてしまう**。役割ごとに「使ったときの重さ」を変え、
  主力ほど30日を通して戻ってくるようにした（主力0.45／処方0.8／その他1.0）
- 候補プールの下限も引き上げた（26 → `count*4+6` と34の大きい方）
- **実測**: ⭐指定した悩みの主力種目は、30日で**各9回**登場（個別化は維持）
- 生活習慣・季節・ゴール・体調（月経/産後）の各リストにも新種目を反映

### あわせて修正した既存の不具合
- **手順イラストで、部位ラベルが手アイコン(✋)の下に隠れて読めなかった**
  （既存13箇所＋上半身系4箇所）。ラベルに白フチを入れ、手の位置を外側にずらして解消
- **カタログページ(catalog.html)が68種のハードコードだった**ため、
  v6.2で足した8種が一覧に出ていなかった。データから自動生成する方式に変更し、
  未分類の取りこぼしが出ない安全網も追加
- トップの「表情筋トレ ◯種」もデータから反映するようにした（数字だけ古くなるのを防ぐ）

### 検証
- 120種すべてについて24項目の整合性テスト（必須項目・手順データ・HTML生成・SVGタグ・
  禁忌・表現チェック）に合格
- 禁忌をお持ちの方でも十分な種目が残ることを確認（全禁忌でも54種）
- 実機で手順パネル・カタログ・モーダルの表示を確認

## v6.12 — 手順イラストを「実在の女性」の写実イラストへ全面刷新（2026-09-14〜）

**イラストの品質への指摘が続いていたため**、手順パネル・アニメ再生・一覧カードの絵を、
ピンクの記号的な顔（SVG）から、実在の女性を描いたような写実寄りのイラストへ置き換える。
生成は Codex CLI の画像生成（ChatGPT の契約枠内・**追加課金ゼロ**）。

### 設計
- **1人の女性・同じ画風・同じ構図**を全665パネルで保つため、基準画像（顔／上半身）を1枚ずつ作り、
  毎回それを添付して生成する（キャラクター調・アニメ調は不採用。実在の人物のような比率・陰影）
- **動きが確実にわかる**ように2段構え:
  1. プロンプトで「基準画像と比べて誰が見ても分かるほど大きく動かす」＋部位ごとの見た目の変化を具体指定
     （例: 眉上げ→「眉が明らかに高く、目が見開かれ、額に横ジワ」）
  2. 向き・場所の矢印はAIに描かせず、**既存の手順データ（座標付き）を画像の上に重ねる**。
     基準画像の顔ランドマークを実測して座標変換を較正（誤差 約12〜16px／1024px）。上半身は区間補間
- 「戻す」「完了」など絵の無いステップは共有画像（無表情／微笑み）を使い、生成枚数を665→538に削減
- 画像は 512px WebP（約12KB/枚、全体で約7MB）。`illust/steps/<種目ID>-<連番>.webp`

### 壊さないための仕組み
- 生成済み一覧（`illust/steps-index.js`）を静的に持ち、**種目の全ステップが揃った種目だけ**画像表示に切り替える
  （揃うまでは従来のSVG。1つの種目内で画像とSVGが混ざらない／存在しない画像を要求しない）
- 画像の読み込みに失敗した場合も、その場でSVGに戻る（`onerror`）
- 手順の文とパネル番号は同じ `steps[]` から出るため、画像化しても「①の絵＝①の文」は崩れない

### 結果（2026-09-14 完了）
- **生成 539枚（顔503＋上半身36）＋基準2枚。120種目すべてが写実イラストに切り替わった**
- 所要: 約9時間（1枚 約160秒・5並列）。追加課金0
- 安全判定で1枚（口の中に親指を入れる動作）が拒否 → 「口を閉じ、外側から指を添える」見え方に表現を変えて再生成し解決
- **顔位置検査（MediaPipe で目の位置を基準画像と比較・512px）**: 顔503枚 中央値0.7px／90%が7.1px以内、
  上半身37枚 中央値0.9px／90%が11.3px以内 → 同一人物・同一構図が全枚で保たれている
- 30px超の28枚はすべて「首・あご・頭を動かす／手で目を覆う」動作そのもの。これらは正面基準の
  重ね矢印がズレるため、**画像だけを見せて矢印を省く**（`OVERLAY_SKIP`・検査結果から自動生成）
- 画像 541枚・7.6MB（1枚 約14KB）。1種目を開いたときの読み込みは約6枚＝約85KB

## v6.13 — 解析後ページのデザイン刷新（文字を減らし、絵で伝える）（2026-09-15）

ターゲットが女性のため、**説明文で伝えるページから、写真と余白で伝えるページ**へ作り替えた。
表示するのは次の5つだけにして、それ以外は撤去した。

| # | ブロック | 主役 |
|---|---|---|
| 01 | あなたの顔タイプ ＋ 顔解析オーバーレイ | 解析線入りの自分の顔（大きく）／タイプ名（特大） |
| 02 | 改善の優先順位 | 順位の数字・テーマ名・優先度バー |
| 03 | 30日間プログラム（今日＋全体） | 手順イラストのタイル |
| 04 | 変化 | スコア推移グラフ・Before/After |

### 撤去したもの（文字量の主因）
- 21項目の指標リスト／左右独立スコア／顔の豆知識／お悩み照合の詳細ブロック／印刷ボタン
- 「STEP 02〜07」の番号見出しと、その下の説明文 → 英字の細いラベル＋短い一文に置換

### 残した情報の扱い
- 優先順位の根拠（写真から見えた特徴・働きかける場所）は**畳んで**おき、「くわしく」で開く形に
- 「なぜ今日このメニューか」「プログラムの設計方針」も同様に畳む
- 撮影条件の警告は、正確さに関わるため**そのまま表示**

### デザイン
- 章立てを「カードの箱」から**罫線と余白**に変更。1章＝1メッセージ
- 今日のメニューを写真主役の正方形タイルに刷新（説明文を出さず、絵と種目名だけ）
- 顔タイプを最大級の文字にし、スコアは「今の位置」を示す小さな指標に降格
- 履歴カードの削除ボタンにスタイルが無く素のボタンが出ていたのを、サムネ右上の控えめなアイコンに修正
- 筋肉名が縦に崩れていたのをチップ表示に修正（平易な名前を主・専門名を従）

### 検証
- スマホ(375px)・デスクトップで横スクロールなし
- 種目モーダル／日別モーダル／Phaseタブ／くわしく／もう一度解析 の操作でエラーゼロ
- 撤去した要素に書き込む処理は、要素が無くても落ちないようガード済み（旧経路の非回帰）

## v6.14 — 解析前ページのデザイン刷新（イラストで伝える）（2026-09-15）

v6.13で解析後を刷新したのに合わせ、**入口（トップ〜写真アップロード）も同じ思想**で作り替えた。

### ヒーロー
- 記号的なイラスト（SVGの顔）を、**手順イラストと同じ写実の女性**に差し替え。
  丸く抜いた写真のまわりに「左右差／たるみ／むくみ」のチップを浮かべ、
  **何を見てくれるのかを説明文なしで**伝える
- コピーを大幅に短縮：「468点の顔ランドマークをAIが解析し…」の2行 →
  **「今日の顔から、30日後の私へ。」＋「写真を1枚。あとは毎日3分。」**
- 「はじめる」CTAを追加して、最初の行き先を1つに絞った
- 数字4つ（468 / 21 / 120 / 30）は白いカードをやめ、罫線だけの静かな帯に

### 入力を 01 → 02 → 03 の3ステップに整理
| | 内容 | 変更点 |
|---|---|---|
| 01 PHOTO | 写真を選ぶ | 大きな円形ボタン1つに。撮影のコツは**4つのアイコンタイル**へ（文章をやめた） |
| 02 CONCERNS | お悩みチップ | 説明2行 → 1行。自由記述は畳んで「その他のお悩みを書く」に |
| 03 ABOUT YOU | 年代・時間・目的 | 表に出すのはこの3つだけ。生活習慣・体調・時間帯の**7項目は畳んだ** |

- 「STEP 01」の番号カードと、その下の説明文を廃止 → 丸番号＋英字ラベル＋短い見出しへ
- 右サイドの「このツールでわかること」「Let's Start!」カードは、
  ヒーローのチップと数字で同じことが伝わるため撤去
- アカウント欄の長い注意文（端末内保存の説明）も畳んだ

### スマホ最適化
- ヒーローは写真を上・文字を下に並べ替え（`order`）、CTAは画面幅いっぱい
- 撮影のコツは2×2、数字は2×2。チップのタップ領域は44px以上を確保
- 375px / 1280px で横スクロールなしを確認

### 検証
- チップ選択・⭐優先・詳細設定の展開・写真選択・解析実行まで通してエラーゼロ
- 既存のID（file-face / symptom-chips / hear-row 10項目 など）は
  すべて維持しているため、JS側の処理は変更なし

## v6.14.1 — スマホの表示崩れを修正（2026-09-15）

### ① CSSが更新されず、古いデザインのまま表示されていた（本命）
- `index.html` が `style.css?v=10` を固定で読んでおり、**v6.13・v6.14 でCSSを変えても
  バージョンが据え置き**だった。ブラウザは古いCSSをキャッシュから使い続けるため、
  一度でも開いたことのある端末では**新デザインの一部が当たらず崩れて見えていた**
- 版番号を**CSSの内容から自動生成（内容ハッシュ）**する方式に変更。
  以後、CSSを変えれば版が自動で変わり、更新忘れによる崩れが起きない

### ② ヒーローのピンクの円が、器からはみ出して下の文字に重なっていた
- 円は「器の幅の96%＋正方形」指定だったが、器の高さは中身（写真＋筆記体）で決まるため、
  **円のほうが73px高く**なり上下にあふれていた
- 器自体を正方形（`aspect-ratio: 1/1`）にし、円を器いっぱい（`inset: 0`）に変更。
  **実測: はみ出し 上下37px → 0px**

### ③ お悩みチップの ☆ が、上の行のチップに重なっていた
- ☆ はチップの外側（上に8px）に出る作りなのに、行間が8pxしかなく**ちょうど接していた**
- 行間を20〜22pxに広げて、☆ のぶんの逃げを確保

（いずれもスマホ375px・デスクトップ1280pxで、横スクロールなし・重なりゼロを実測で確認）

---

## 参考: 最高峰化プロジェクト以前の主な変更

| バージョン | 内容 |
|---|---|
| v6.3.6 | 今日のトレーニングをガイドフロー化（次へを押すだけで完走） |
| v6.3.5 | PWA化で翌日の再訪導線（ホーム画面に追加・SWはCDN資産のみキャッシュ） |
| v6.3.4 | 顔検出失敗を「行き止まり」から「やり直し導線」に |
| v6.3.3 | サインインを「開始の瞬間」に移動（価値を見せてからメールを求める） |
| v6.3.2 | 写真選択後のスティッキー解析CTA |
| v6.3.1 | AIモデル先読みで初回解析の待ち時間をほぼゼロに |
| v6.3 | メール式サインインで個人ページ化（データは端末内・アカウント別） |
| v6.2.2 | 写真なしで前回の診断結果・30日プログラムを復元 |
| v6.2.1 | モバイル優先トグル/結果の平易化/上半身図/法的モーダル/種目76種 |
| v6.2 | 採点較正・30日ジャーニー・実行可能性15件・確定バグ10件修正 |
