# FACE_AI_AUDIT.md — FaceLab 品質監査

> 監査日: 2026-08-11 ／ 対象: v6.3.6 (commit 6ec872a)
> 方法: 全ファイル精読 + node で合成顔を投入した実測検証 + 主要指摘の敵対的再検証

## 総合スコア: 46.3 / 100

| 次元 | 配点 | 実点 | 主因 |
|---|---|---|---|
| ①顔写真分析品質 | 15 | **5** | 計測層が「同じ顔で結果が再現しない」 |
| ②本人の悩みとの統合 | 15 | **6.5** | 統合ではなく併記。照合が存在しない |
| ③パーソナライズ | 15 | **6.5** | ⭐優先・生活習慣・履歴が実測でほぼ無効 |
| ④セルフケア・運動選択品質 | 15 | **8.5** | 76種の中身は良好、メタデータ不足 |
| ⑤30日プログラム設計 | 10 | **4** | 途中評価・調整機構が存在しない |
| ⑥説明+⑧UI/UX+⑨安全性 | 20 | **11** | 医療的断定が全問題カードに存在 |
| ⑦継続性 | 5 | **2.3** | 記録が Day 完了フラグのみ |
| ⑩速度・安定性 | 5 | **2.5** | 境界ケース未対応、app.js 2000行 |

---

## S級: 致命的問題（すべて実測で確認・すべて追加課金0円で修正可能）

### S1. アスペクト比の異方性を無視 → 全指標が最大78%変動
MediaPipe の正規化座標は x=幅除算 / y=高さ除算で単位が異なるのに、`analyzer.js` の
`dist()` は等方空間として距離計算している。アスペクト補正はコード全体に存在しない。

**実測**: 同一の顔でアスペクトのみ変更（1:1 → 4:3 → 16:9）
- faceWHRatio 1.239 → 0.929 → 0.697（きっかり ×4/3・×16/9 の純粋な代数）
- philtrumIdx 0.160 → 0.213 → 0.284
- nasolabialIndex 0.363 → 0.473 → 0.623
- 検出される問題が「口角下がり(軽)+ほうれい線(中)+むくみ(重)」→「口角下がり(重)+下顔面の縦長感(中)」に入れ替わり
- スコア 60(C) → 71(B)

横÷横の mandibleProminence と fiveEyesIdx だけ3ケース完全一致 → 原因がアスペクトである裏付け。
**同じ顔でもトリミング次第で結果が変わり、30日 Before/After 比較の前提が成立しない。**

### S2. eyeHeightDiff が構造的に常に 0（目の左右差が原理的に検出不能）
`normalizeLandmarks()` は両目中点を水平化するが、`eyeHeightDiff` は同じ両目中点から
再計算しているため恒等的に 0。**実測で全15ケース `0.000e+0`、目を1mm上げた検証でも 0。**

代わりに rollDeg / browHeightDiff / mouthTilt / midlineTilt が発生し、
asymmetryScore 13.62 で「左右非対称」を検出 →
**実在する目の左右差が、眉と口の"偽の左右差"に転嫁されている。**
app.js のオーバーレイの目ラインも永久に緑固定。

### S3. roll 補正を異方空間で行うため、首の傾きで誤診
**実測**: roll=8° を正方形画像で入れると asymmetryScore 0.00 / スコア71(B) なのに、
同じ姿勢を 3:4 縦写真にすると midlineTilt 4.596 / asymmetryScore 18.39 で
「顔の左右非対称(中度)」が誤検出されスコア 52(D)。roll=15° では asymmetryScore 33.19 で重度。
**完全に対称な顔に「噛み癖・寝姿勢が原因」と告げる。**

### S4. 写真から測定不能な身体内部を断定（絶対条件に直撃）
`detectProblems()` の description / tissues が、写真では判定できない内部状態を断定：
- 「リンパの停滞」「頬の脂肪体が下方に落ちている可能性」「咬筋が肥大している可能性」
- 「頬筋の萎縮」「広頸筋・咬筋の過緊張／舌骨上筋群の弱化が要因」「上眼瞼挙筋の動員不足」
- 全問題カードで具体的筋肉名を「🔴 こわばっている所」「🟡 使えていない所」として断定表示

---

## A級

| # | 問題 | 実測・根拠 |
|---|---|---|
| A1 | pitch（あおり・うつむき）を推定も補正も警告もしていない | ±10°で mouthCornerDrop が16倍、口角下がりが low↔high で反転。自撮り最頻角度が未対策 |
| A2 | yaw 推定式が実質「鼻の高さ計」 | 真値10°→報告5.31°。12°警告は実質22°相当。推定するだけで補正に未使用 |
| A3 | 表情を検出しても指標を無効化しない | 笑顔でほうれい線が mid→high に悪化。2mm の微笑みは検出すらされない |
| A4 | 撮影品質の事前ゲートが皆無 | 解像度・顔サイズ・ブレ・白飛びの検査なし。警告は結果より後ろに配置 |
| A5 | ⭐優先が30日をほぼ変えない | Day1 完全一致、30日通算で +1枠のみ。UI は「集中設計」と表示 |
| A6 | 写真結果と悩みの照合が存在しない | 一致/不一致の提示ゼロ。写真検出済みの悩みは黙って捨てられる |
| A7 | priorityKeys の slice(0,3) がキー数で切る | ⭐3つ選んでも3つ目が丸ごと落ちる |
| A8 | 生活習慣4問がメニューに 0% 影響 | 実測 変化日数 0/30。なのに理由文は「定期的に挟みます」と断言 |
| A9 | 途中評価（Day7/14/21）が存在しない | app.js 該当コード 0 件 |
| A10 | 実施記録が Day 完了フラグのみ | できた/一部/できなかった・体感・違和感の記録なし |

## B級
- 478点中28点(5.9%)しか使用せず。虹彩10点(絶対mmスケール)・blendshape52種・head pose 4x4・z座標が全て未使用
- `COUNT_BY_TIME` が時間比例していない（15分設定で実測約7分）
- `buildDayReason` は30日でユニーク3種。その日の種目名に一言も触れない
- `pickLeastUsed` のボーナス最大 -2.0 を使用回数 +2 が打ち消し、実質 round-robin

---

## 削除・簡略化候補

| 対象 | 理由 |
|---|---|
| `scoreToPercentile` / `erf` / meanMap | 根拠のない集団平均。「上位X%」は顔面偏差値的設計 |
| S/A/B/C/**D** グレード | 学校の成績記号。特に D は落第の含意 |
| `sevPct`（85/55/30） | 3段階から機械生成した定数を大きく表示＝偽の精度 |
| `partsBalance`（三庭・五眼） | 骨格判定で表情筋トレでは変わらない。最も顔面偏差値的 |
| `widthHeightRatio > 5` | 閉口時の実測 12〜24 で常に真の死条件 |
| 死に定数7個 (NASOLABIAL_L/R 等) | grep 0ヒット |
| `pickTodayMenu` / `unmarkDayDone` / `diffSnapshots` | 参照0件のデッドコード |
| 重複チップ | 「眉の高さの左右差」は「目の左右差」と結果が1文字も変わらない |
| knowledge.js「むくみの90%はリンパの停滞」 | 出典なき数値＋医療的断定 |
| 「468点」表記 | 実際は虹彩込み478点 |

---

## 現在優れているところ（維持すべき資産）

1. **76種のセルフケアDBと絵と文の1ソース化** — 手順は `steps[]` 1箇所のみで構造的に一致
2. **禁忌フィルタが本物** — 実測で contra 5つ全選択が30/30日・95枠を書き換える
3. **撮影距離への完全な不変性** — mm/px を 0.22→0.5 で全21指標が完全一致
4. **計測不能を null で返す実装** — 画像端で wrinkleIdx を null 化し危険をコメントに明記
5. **煽らない配慮** — スコア下限45、percentile は上位50%以内のみ、必ず強みを1つ返す
6. **端末内完結のプライバシー**と PWA・ガイドフロー等の継続導線
